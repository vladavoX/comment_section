interface Props {
  user: any
  setUser: React.Dispatch<React.SetStateAction<any>>
}

const LoginRegisterPage = ({ user, setUser }: Props) => {
  return <div>LoginRegisterPage</div>
}

export default LoginRegisterPage
